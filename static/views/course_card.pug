div.card
    h2 #{course.name}
    p Total Rating: #{course.rating}/5
    p #{course.description}
    if isUserLoggedIn
        button(type='button') Like
    a(href=`/api/courses/${course.id}`).link View course
